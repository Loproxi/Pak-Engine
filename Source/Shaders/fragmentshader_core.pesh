#version 330 core

in textureCoords

out vec4 FragShaderColor;

uniform sampler2D texture0;

void main()
{
    FragShaderColor = texture(texture0,textureCoords) * vec4(textureCoords.x,textureCoords.y,textureCoords.z,1.0f);
}
